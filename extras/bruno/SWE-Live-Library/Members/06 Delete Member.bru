meta {
  name: 06 Delete Member
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/rest/members/{{createdMemberId}}
  body: none
  auth: none
}

tests {
  test("returns HTTP 204 No Content", function() {
    expect(res.status).to.equal(204);
  });
}
