meta {
  name: 03 Search Members By Query
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/rest/members?username=galaxy_devron
  body: none
  auth: none
}

params:query {
  username: galaxy_devron
  ~emailAddress: 
  ~lastName: Menke
  ~limit: 20
  ~offset: 0
}

tests {
  test("returns HTTP 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("returns a JSON array", function() {
    expect(res.body).to.be.an("array");
  });
}
